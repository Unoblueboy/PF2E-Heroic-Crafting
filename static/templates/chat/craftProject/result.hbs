<div class="pf2e chat-card"
     data-craft-project-results
     data-craft-details="{{craftDetails}}"
     data-actor-uuid="{{actorUuid}}"
     data-project-id="{{projectId}}">
    <div class="card-content">
        <div style="display: flex; align-items: center; margin-bottom: 3px;">
            <img src="{{item.img}}"
                 style="height:32px; width:32px; margin-right: 3px;">
            <span>{{{itemLink}}}</span>
        </div>
        <hr>
        <div class="spent-materials-list">
            {{#each materials as | material |}}
                <div class="spent-material">
                    {{#with material.item as | item |}}
                        <img src="{{item.img}}" />
                        <span class="spent-material-item-name">{{#if material.quantity}}{{material.quantity}}x
                            {{/if}}{{item.name}}</span>
                    {{/with}}
                    <span class="spent-material-value">{{#if material.quantity}}{{material.quantity}}x
                        {{/if}}{{material.spent}}</span>
                </div>
            {{/each}}
        </div>
        <p>Total Cost: {{totalMaterialsSpent}}</p>
    </div>
    <hr>
    <div class="card-buttons"
         data-visibility="owner">
        {{#if (eq outcome "criticalFailure")}}
            <button type="button"
                    data-outcome="{{outcome}}">Deduct Cost from the Project Value</button>
        {{else if (eq outcome "failure")}}
            <button type="button"
                    data-outcome="{{outcome}}">Add half the Cost to the Project Value</button>
        {{else}}
            <button type="button"
                    data-outcome="{{outcome}}">Add double the Cost to the Project Value</button>
        {{/if}}
    </div>
</div>